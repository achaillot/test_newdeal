<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Accueil</a>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <a class="nav-link" href="{{ path('article_index')}}">Articles</a>
  </div>
  {% if not app.user %}
    <div>
      <a href="{{ path('app_login') }}" role="button" class="btn btn-success">Connexion</button></a>
    </div>
  {% endif %}  
  {% if app.user %}  
    <div>
      <a href="{{ path('app_logout') }}" role="button" class="btn btn-danger">Se déconnecter</button></a>
    </div>
  {% endif %}
</nav>